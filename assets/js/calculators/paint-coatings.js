import { createStubCalculator } from './_stub.js';

export const calculator = createStubCalculator({
  id: 'paint-coatings',
  name: 'Paint & Coatings',
  description: 'Evaluate substrate prep, primer, and finish coats across interior and exterior scopes.'
});
