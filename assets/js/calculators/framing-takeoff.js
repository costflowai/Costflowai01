import { createStubCalculator } from './_stub.js';

export const calculator = createStubCalculator({
  id: 'framing-takeoff',
  name: 'Framing Takeoff',
  description: 'Estimate studs, plates, and headers for wood framing at 16/24" on-center spacing.'
});
