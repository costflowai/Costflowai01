import { createStubCalculator } from './_stub.js';

export const calculator = createStubCalculator({
  id: 'firestopping',
  name: 'Firestopping',
  description: 'Capture penetrations by type to assign UL systems, sealants, and hourly ratings.'
});
