import { createStubCalculator } from './_stub.js';

export const calculator = createStubCalculator({
  id: 'site-concrete',
  name: 'Site Concrete',
  description: 'Plan curbs, gutters, and sidewalk pours with forming, finishing, and reinforcement.'
});
