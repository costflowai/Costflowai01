import { createStubCalculator } from './_stub.js';

export const calculator = createStubCalculator({
  id: 'masonry',
  name: 'Masonry Systems',
  description: 'Quantify CMU, brick, mortar, reinforcing, and grout lifts for vertical assemblies.'
});
