import { createStubCalculator } from './_stub.js';

export const calculator = createStubCalculator({
  id: 'doors-windows',
  name: 'Doors & Windows',
  description: 'Summarize openings, frame types, glazing, and hardware for door and window packages.'
});
